# Installation

Install Tampermonkey, create a new script, paste `deepseek_observer.user.js`, and save. The metadata limits it to `https://chat.deepseek.com/*`, uses `@run-at document-start`, and requests `@grant none`.

Do not add GM networking, cookie, storage, or download permissions. Normal browser downloads implement export.
