# IndexedDB history schema v5

## Direct observation
`indexedDB` lists database `deepseek-chat`, version 1, and store `history-message` with `keyPath: null`, `autoIncrement: false`, and no indexes. Its approximate count is `NOT_READ`.

## Record schema result
**NOT OBSERVABLE in this capture.** No records, cursor samples, field names, keys, content types, message/conversation references, roles, timestamps, or metadata were exported.

`devtools_observer/phase5_indexeddb_observer.js` offers read-only bounded schema observation: count plus up to 25 record **field/type/size** profiles, never values. It must be run in a normal account-owner browser session to upgrade this report.
