# Phase 2 file pipeline

No synthetic attachment scenario (E01–E08) has completed. Therefore MIME support, local preprocessing, upload initiation, attachment metadata, progress, server-side object creation, conversation association, and generation-context use remain unknown.

```text
Synthetic file → client processing [UNKNOWN]
→ upload [UNKNOWN] → server attachment/object [UNKNOWN]
→ conversation association [UNKNOWN] → generation/context use [UNKNOWN]
```

Capture each independently; record a UI rejection as an observation and never change/replay a request to force acceptance.
